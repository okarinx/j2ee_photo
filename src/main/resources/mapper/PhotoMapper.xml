<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.project.dao.PhotoMapper">
    <resultMap id="BaseResultMap" type="com.company.project.model.Photo">
        <!--
          WARNING - @mbg.generated
        -->
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="display_name" jdbcType="VARCHAR" property="displayName"/>
        <result column="file_url" jdbcType="VARCHAR" property="fileUrl"/>
        <result column="country" jdbcType="VARCHAR" property="country"/>
        <result column="position" jdbcType="VARCHAR" property="position"/>
        <result column="uploader_name" jdbcType="VARCHAR" property="uploaderName"/>
        <result column="bjs_take_photo" jdbcType="DATE" property="bjsTakePhoto"/>
        <result column="coordinates" jdbcType="VARCHAR" property="coordinates"/>
        <result column="scale" jdbcType="VARCHAR" property="scale"/>
    </resultMap>


    <select id="selectCountry" parameterType="java.lang.String" resultType="java.lang.String">
    SELECT country
    FROM photo
    GROUP BY country
  </select>

    <select id="selectScale" parameterType="java.lang.String" resultType="java.lang.String">
    SELECT scale
    FROM photo
    WHERE scale NOT LIKE 'undefined' AND scale NOT LIKE 'null'
    GROUP BY scale;
  </select>

    <select id="selectLastId"  resultType="java.lang.Integer">
    SELECT id
    FROM photo
    ORDER BY id DESC
	LIMIT 1;
  </select>

    <select id="selectFavorPhotoList" parameterType="java.lang.String" resultMap="BaseResultMap">
    SELECT photo.id,photo.display_name,photo.file_url,photo.country,photo.position,photo.uploader_name,photo.bjs_take_photo,photo.coordinates,photo.scale
    FROM photo JOIN favors ON photo.id = favors.photo_id
    where favors.user_name=#{username,jdbcType=VARCHAR};
  </select>
</mapper>